{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php-fpm -D -c /app/php-fpm.conf && nginx -c /app/nginx.conf",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}